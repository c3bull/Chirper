import React from "react";

const Chat = () => {
    return (
        <div id="chatDiv">
            <textarea>
              Cześć, oto przykład tekstu w polu tekstowym.
            </textarea>
        </div>
    );
};

export default Chat;
